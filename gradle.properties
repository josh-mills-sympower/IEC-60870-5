# Gradle performance settings
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.jvmargs=-Xmx2048m -Xms512m

# Repository credentials (set these in ~/.gradle/gradle.properties for security)
# sonatypeRepository=https://oss.sonatype.org/service/local/staging/deploy/maven2/
# sonatypeSnapshotRepository=https://oss.sonatype.org/content/repositories/snapshots/
# sonatypeUser=your-username
# sonatypePass=your-password

# Signing configuration (set these in ~/.gradle/gradle.properties for security)
# signing.keyId=your-key-id
# signing.password=your-password
# signing.secretKeyRingFile=/path/to/secring.gpg